We're in a repo where we have an electron app for code review, that has caching for bitbucket to avoid rate limitng. I want to focus on cleaning up the PR inbox, so it's clear what data is cached or not, if any cahcing is going in background;, and also to generally smoothen the workflow for development. We often get cases where the elecrtron app gets stuck; or Error invoking remote method 'desktop:invoke': Error: Rate limit for this resource has been exceeded - when loading a diff, etc. Our goal is to improve our stable foundations, then commit and push incrementally as you go until we have a clear set of scripts and skilsl to use that are 100% reliable
